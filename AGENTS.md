# Knitspace 项目协作规则

## 目标与决策

- 以本地优先的个人数字工作台为产品边界，优先打通真实工作流、稳定性、数据安全和功能可发现性。
- 修改前先阅读相关入口、调用链、测试和数据边界；旧文档与当前实现冲突时，以可复现验证为准，并同步更新文档。
- 新功能应接入五空间导航、全局搜索或既有工作流，避免新增难以发现、重复或只有展示作用的页面。

## 前端与体验

- 复用现有设计变量、通用组件和交互 helper。较大的视觉或信息架构调整应形成跨页面方案，并在真实桌面尺寸、明暗主题和长内容下验证。
- 设计技能、研究方法和外部参考按任务适配度选择；采用前结合 Knitspace 的中文桌面场景、本地优先定位、可访问性、性能和现有组件体系判断。
- 右键菜单同时覆盖鼠标、`Shift+F10`/菜单键、Escape、焦点恢复和视口边缘夹紧；关键状态不能只靠颜色表达。

## 数据、命名与兼容迁移

- SQLite、Markdown Vault 和资源目录是桌面数据边界；Pinia 只保留适合界面使用的轻量状态和按需缓存。
- 新增或调整持久化数据时，同时处理 schema 迁移、浏览器数据导入、备份/恢复、FTS、附件清理、失败恢复、数据量上限和公开版 feature gate。
- 产品名、协议名、存储键、目录、凭据服务和备份格式都可以演进。变更前盘点生产者、消费者和历史数据，通过双读或一次性迁移、版本化测试、升级验证和回滚步骤完成收敛。
- Rust 处理任务的重启恢复放在一次性 hydration/startup 边界，不放入普通 command 会反复调用的 `VaultService::open()`。

## 性能与安全

- 大 Markdown、PDF、图片、媒体和长列表优先使用 Worker、懒加载、分页、虚拟列表、可见区渲染与有界缓存；避免把整个 Vault 或大型二进制放入响应式状态。
- 不在源码、日志、终端输出、备份或提交中暴露 API Key、用户路径和私人数据。远程 AI 请求仅发送用户明确选择的内容。
- 文件处理默认生成新输出，不覆盖输入；破坏性操作提供明确目标、确认、恢复路径或可回滚步骤。

## 修改、协作与验证

- 保留工作区中的既有修改，不执行会丢失用户工作的重置或覆盖操作。编辑保持范围清晰，并在完成后检查 diff。
- 可根据任务规模独立完成或并行拆分。并行任务需要明确接口、文件所有权和验收标准，最终由主流程统一审阅、验证和整合。
- 测试强度与风险匹配：纯逻辑修改运行相关单测；跨模块修改补充完整前端测试和构建；Rust、迁移、公开版或桌面能力修改增加对应 Cargo、feature gate、安装或真实运行验证。
- 测试通过只证明覆盖范围内的行为，不等同于真实桌面工作流或正式发布已经完成；交付说明应区分自动验证、人工验证和待验证项。

